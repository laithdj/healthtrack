import { DateTimeDOPipe } from './date-time-do.pipe';

describe('DateTimeDOPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeDOPipe();
    expect(pipe).toBeTruthy();
  });
});
